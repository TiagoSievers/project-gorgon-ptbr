# ══════════════════════════════════════════════════════════════
# Словарь ключей для regex-групп захвата (patterns.keys.yaml)
# Companion-файл для patterns.regex.yaml
#
# Когда regex-правило захватывает $1, $2, ... — каждая группа
# ищется в этом файле и заменяется переводом.
# Если ключ не найден — остаётся оригинал.
#
# Формат:  EnglishWord: Перевод (с учётом склонения в контексте)
# ══════════════════════════════════════════════════════════════

# ── Существа (для "Search Corpse of X", "Talking with X" и т.д.) ──
# Именительный / общая форма — подходит для большинства контекстов

# Животные
Pig: свиньи
Cow: коровы

# Название скилов
Mycology: микология